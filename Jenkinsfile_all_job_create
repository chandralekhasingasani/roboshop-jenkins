pipeline{
    agent any
    stages{
        stage('create'){
            steps{
                sh 'ansible-playbook main.yml --connection=local --inventory 127.0.0.1 --limit 127.0.0.1'
            }

        }
    }
}